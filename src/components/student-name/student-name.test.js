describe("Student Name Component Tests", () => {
    it.todo("Renders a Student Name component");
    it.todo(
        "Tests if the Student full-name has turned to the upper case letters"
    );
});
