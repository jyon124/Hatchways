describe("Student Profile Component Tests", () => {
    it.todo("Renders a Student Profile component");
    it.todo(
        "Tests if average grades have calculated properly with two decimal places"
    );
});
