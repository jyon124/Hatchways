describe("Student Cards Container Component Tests", () => {
    it.todo("Renders a Student Cards Container component");
    it.todo("Renders Student Card components when students data are available");
    it.todo(
        "Test if each student data properly init.todoialized wit.todoh empty tag array"
    );
    it.todo("Renders StudentCards that only match the name search term");
    it.todo("Renders StudentCards that only match the tag search term");
    it.todo(
        "Renders StudentCards that only match the search & tag search term"
    );
    it.todo(
        "Check if StudentCards are sorted by most relevant to the name search input"
    );
    it.todo(
        "Check if StudentCards are sorted alphabetically in case the same name search term exists in different cards"
    );
});
